#!/bin/bash
# Soplos Theme Manager launcher wrapper

export WMCLASS="org.soplos.thememanager"

INSTALL_DIR="/usr/share/soplos-theme-manager"
if [ -d "$INSTALL_DIR" ]; then
    cd "$INSTALL_DIR"
else
    cd "$(dirname "$(readlink -f "$0")")"
fi

exec python3 main.py "$@"
